# Production dependencies with flexible versioning
# This approach prevents most dependency conflicts

# Web Scraping
beautifulsoup4~=4.12.0
requests~=2.31.0
lxml~=5.1.0

# Data Processing
pandas~=2.2.0
python-dateutil~=2.8.0

# Vector Database
faiss-cpu~=1.7.0
numpy~=1.26.0

# MCP Server (let pip resolve exact versions)
fastmcp>=0.1.0
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
sse-starlette>=2.0.0

# LLM Integration (let these resolve together)
langchain>=0.1.5
langchain-community
tiktoken>=0.5.0

# Utilities
python-dotenv~=1.0.0
pydantic>=2.7.0
pydantic-settings  # Let fastmcp determine the version
loguru~=0.7.0

# HTTP client
httpx~=0.26.0

# Using ~= means "compatible version"
# - ~=1.4.2 means >=1.4.2, <1.5.0
# - This allows patch updates but not minor version changes
# Using >= allows any newer version that's compatible
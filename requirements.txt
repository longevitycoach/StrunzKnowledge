# Core MCP SDK Dependencies
mcp>=1.0.0

# Vector Database
faiss-cpu>=1.7.4
numpy>=1.26.0
sentence-transformers>=2.2.0

# Web Scraping (for data collection)
beautifulsoup4>=4.12.0
requests>=2.31.0
lxml>=5.1.0

# Data Processing
pandas>=2.2.0
python-dateutil>=2.8.0

# Testing Dependencies
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.28.0
scikit-learn>=1.3.0
psutil>=5.9.0

# Utilities
python-dotenv>=1.0.0
pydantic>=2.7.0

# Optional: FastMCP for fallback (not installed to avoid Railway conflicts)
# These dependencies are intentionally excluded to prevent deployment issues:
# - fastmcp (requires FastAPI stack)
# - fastapi (web framework not needed for MCP stdio protocol)  
# - uvicorn (ASGI server not needed for MCP stdio protocol)